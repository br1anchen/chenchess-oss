# The stylesheets that stay

The live rule and closed list live in `CODING_STANDARDS.md` (Permanent CSS).
A stylesheet not on that list is a defect, and adding one means adding its
row there with the reason in the same change.
